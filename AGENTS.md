# MindX Project AGENTS

## Memory Tab Guardrail

- 改 `Memory` 视觉和交互前，先读 `docs/lessons/index.md`，再对照 `2.0` 的骨架做实现。
- 纯展示层模块只允许“直接复刻骨架 + 接 1.0 数据”，不允许再做混合中间态。
- 生产 UI 里不要保留示例卡片、说明性占位文案、重复入口或完成态里的进行态 badge。
- 只要 review 发现卡片参差、入口重复、状态词不一致，就先停下来重做验收，不要带着“差不多”继续交付。
